steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/gpt-candidate-sheets/gpt-cloud-agent:latest', '.']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/gpt-candidate-sheets/gpt-cloud-agent:latest']

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'gpt-cloud-agent'
      - '--image'
      - 'gcr.io/gpt-candidate-sheets/gpt-cloud-agent:latest'
      - '--region'
      - 'europe-west1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'

images:
  - 'gcr.io/gpt-candidate-sheets/gpt-cloud-agent:latest'

options:
  logging: CLOUD_LOGGING_ONLY
